<script language="javascript">
  $(function() {
      $(".datepicker").datepicker({
          format: "dd/mm/yyyy",
          language: 'pt-BR'
      })
  })
</script>



<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
   Escolha o livro:<br>
   <%= f.select :livro_id, options_from_collection_for_select(Livro.all, :id, :titulo, selected: f.object.livro_id),include_blank:true  %>

  </div>

  <div class="field">
    <%= f.label :data %> <br>
    <%= f.text_field :data, class: 'datepicker' %>     <!--, value: Date.today, readonly: true %> -->
  </div>

  <div class="field">
    <%= f.label :pagina %><br>
    <%= f.text_field :pagina %>
  </div>

   <div clas="field">
     <%= f.check_box(:livrocompleto) %>
     <%= f.label(:livrocompleto, "Leitura concuída") %>
   </div>

  <div class="actions">
    <%= f.submit "Enviar" %>
  </div>
<% end %>
